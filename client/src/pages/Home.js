import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Welcome to Car Maintenance Tracker</h1>
            <p>Keep track of your car's maintenance history with ease.</p>
            {/* Add more content here, such as car listings, recent maintenance records, etc. */}
        </div>
    );
};

export default Home;
