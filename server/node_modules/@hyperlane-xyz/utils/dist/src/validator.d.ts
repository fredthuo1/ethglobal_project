import { ethers } from 'ethers';
import { Address, Checkpoint, Domain, HexString, S3Checkpoint, SignatureLike } from './types';
/**
 * Utilities for validators to construct and verify checkpoints.
 */
export declare class BaseValidator {
    localDomain: Domain;
    address: Address;
    mailbox: Address;
    constructor(address: Address, localDomain: Domain, mailbox: Address);
    domainHash(): string;
    message(checkpoint: Checkpoint, messageId?: HexString): string;
    messageHash(checkpoint: Checkpoint, messageId?: HexString): Uint8Array;
    recoverAddressFromCheckpoint(checkpoint: Checkpoint, signature: SignatureLike, messageId?: HexString): Address;
    matchesSigner(checkpoint: Checkpoint, signature: SignatureLike, messageId?: HexString): boolean;
}
export declare class Validator extends BaseValidator {
    protected signer: ethers.Signer;
    constructor(signer: ethers.Signer, address: Address, localDomain: Domain, mailbox: Address);
    static fromSigner(signer: ethers.Signer, localDomain: Domain, mailbox: Address): Promise<Validator>;
    signCheckpoint(root: HexString, index: number): Promise<S3Checkpoint>;
}
//# sourceMappingURL=validator.d.ts.map